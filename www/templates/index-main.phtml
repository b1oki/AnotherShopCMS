<!DOCTYPE html>
<html lang="ru">
<? require_once 'root-head.phtml' ?>
<body>
<? require_once 'root-header.phtml' ?>
<div class="container">
    <div class="row">
        <div class="three columns" id="sidebar-left">
            <? require 'newest-news.phtml' ?>
        </div>
        <div class="nine columns" id="main-content">
            <div class="row">
                <h3><?= $this->getTitle() ?></h3>

                <p>
                    Наша компания существует на Российском рынке
                </p>
            </div>
            <div class="row">
                <h4>Наша продукция</h4>

                <div class="row">
                    <? foreach ($this->data['categories'] as $category): ?>
                        <div class="four columns product-list-item">
                            <a href="/product/<?= $category['id'] ?>/">
                                <img class="u-full-width" src="
                                <? if (!empty($category['image'])):
                                    echo $category['image'];
                                else:
                                    echo $this->data['category-no-image'];
                                endif;
                                ?>
                                    ">
                                <span><?= $category['title'] ?></span>
                            </a>
                        </div>
                    <? endforeach ?>
                </div>
            </div>
        </div>
    </div>
    <? require_once 'root-footer.phtml' ?>
</div>
</body>
</html>